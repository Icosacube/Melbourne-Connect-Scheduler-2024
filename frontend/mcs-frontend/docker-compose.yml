version: "3.8"

services:
  ts-react-docker:
    build:
      context: .
      dockerfile: Dockerfile
      target: base
    ports:
      - "80:3000"
    volumes:
      - .:/app
    command: npm start