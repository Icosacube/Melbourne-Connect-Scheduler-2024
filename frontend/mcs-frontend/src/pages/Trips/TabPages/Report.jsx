import { Button } from "@mui/material";
import React from "react";

function Report() {
  return (
    <Button className="bg-gray-300 p-8 w-full min-h-60 flex ">
      <Button variant="contained">Generate</Button>
    </Button>
  );
}

export default Report;
